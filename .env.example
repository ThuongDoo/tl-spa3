# Copy to .env and fill in for this specific deployment (one website).
# Firebase itself is shared across all deployments (see src/lib/firebase.js);
# this just points the build at its own websites/{id} doc.
VITE_WEBSITE_ID=
